# Copie para .env na raiz. O arquivo .env está no .gitignore e nunca deve ir ao Git.
# A senha do MT5 (MT5_PASSWORD) é só local — não cole em YAML, journal, studies.json nem no chat.
MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=
MT5_PATH=
